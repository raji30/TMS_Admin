import { OnInit, ElementRef } from '@angular/core';
import { ViewProviders } from './models/ViewProviders';
import 'rxjs/operator/toPromise';
import 'rxjs/operator/takeUntil';
import { EventEmitter } from '@angular/core';
export declare class DocumentViewDirective implements OnInit {
    private iframe;
    private _provider;
    private _originSrc;
    /**
     * 檢視器提供者
     */
    provider: ViewProviders;
    providerChange: EventEmitter<ViewProviders>;
    /**
     * 原始文件連結
     */
    readonly originSrc: string;
    constructor(iframe: ElementRef);
    ngOnInit(): void;
    /**
     * 根據對應的檢視器提供者產生文件網址
     */
    getDocumentUrl(): string;
    /**
     * 轉換連結與編碼
     * @param url 連結
     */
    encodeURI(url: string): string;
}
