/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AccountingoptionsService } from './accountingoptions.service';

describe('Service: Accountingoptions', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AccountingoptionsService]
    });
  });

  it('should ...', inject([AccountingoptionsService], (service: AccountingoptionsService) => {
    expect(service).toBeTruthy();
  }));
});
